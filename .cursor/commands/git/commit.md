# commit command
- use git comamnd to read all current existing changes
- understand and interpret all active changes
- write a proper commit message and commit title 
- confirm with the user about the proposed commit title and message
- when the user consent, perform the git commit command
- all commit messages should all be in lowercase, no capital letters at all
- if we are handling a package, remmeber to update the version by 0.0.1, e.g., if it is 0.1.0, make it 0.1.1 (unless told otherwise)