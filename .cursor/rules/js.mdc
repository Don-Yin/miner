---
globs: *.tsx,*.ts
alwaysApply: false
---
- avoid "try and except" blocks, these are monkey patches, we want to find the true cause and expose the errors
- files must not exceed 256 LOC. if they do, breakdown to reusable utils and components
